meta {
  name: Hello-No-Auth
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/hello
  body: none
  auth: none
}

tests {
  test("should return 403 Forbidden", function() {
    expect(res.status).to.equal(403);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
